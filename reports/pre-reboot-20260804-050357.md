# H100 第一次重启前报告

- Run ID: `20260804-050357`
- 报告时间：2026-08-04 14:07 CST 后完成最终 Gate
- 主机名：`sagsh100server`
- 管理地址：`10.82.36.1/24`，接口 `ens1f0np0`
- 默认路由：`default via 10.82.36.254 dev ens1f0np0`
- 当前 Kernel：`7.0.0-14-generic`
- 下次预计 Kernel：`7.0.0-28-generic`
- 启动项依据：`GRUB_DEFAULT=0`；GRUB 第一项 `Ubuntu` 明确加载 `/vmlinuz-7.0.0-28-generic`

## NVIDIA 与 initramfs

- 驱动分支：NVIDIA 官方 R595 Production Branch
- 已安装版本：`595.91.07-1ubuntu1`
- 内核模块：NVIDIA Open Kernel Modules
- Secure Boot：disabled
- DKMS：当前及下次内核均为 installed
- 新 initramfs：已更新，包含 NVIDIA DKMS 模块及官方包提供的 nouveau blacklist
- 新 initramfs SHA-256：`c55f8246aaba17be34940a21aca2ef839d3046f00f628d051d9854f4918c35b3`
- CUDA Toolkit：未安装

当前旧 boot 仍由 `nouveau` 绑定四张 H100。没有进行在线卸载、GPU reset 或手工强载 NVIDIA。`nvidia-persistenced` 因当前不存在 `/dev/nvidia*` 处于 failed，必须在重启后复核，不能据此提前宣称驱动通过。

## 远程管理可恢复性

- `codexops` 公钥登录：passed
- 公钥指纹：`SHA256:vRb0Vno7tl/hcmiFeDiekEKTx1zsA/oPHvpsSoG3Vwc`
- `sudo -n true`：passed
- `sudo -n id`：`uid=0(root) gid=0(root) groups=0(root)`
- `sshd -t`：passed
- `ssh.service`：enabled、active
- `ssh.socket`：enabled、active
- `codexops` 有效认证策略：公钥 yes，密码 no，keyboard-interactive no
- `origin-al`：账号、密码和原有权限均保留，未关闭
- 防火墙：未配置、未修改
- 最终 Gate 时地址与默认路由均存在

## fstab 与 XFS project quota

- `/etc/fstab` 只在两条既有 XFS 项增加 `noatime,prjquota`
- 设备、挂载点、文件系统类型均未变，未使用 `/dev/sdX`
- `findmnt --verify --verbose`：success，0 errors，0 warnings
- 原始备份：`/srv/gpu-platform/platform/backups/fstab-20260804-050357`
- 当前 live mount：两块 XFS 仍为 `relatime,noquota`
- 预期：经批准重启后加载 `noatime,prjquota`
- 当前状态：`QUOTA REBOOT REQUIRED`
- `PROJECT QUOTA PASSED` 尚未声明

## systemd 与网络风险

当前 failed units：

- `nvidia-persistenced.service`：P1 验收项；当前 boot 没有 NVIDIA 设备节点，重启后复核
- `systemd-networkd-wait-online.service`：P2；本次 boot 等待超时，未修改网络配置

Mellanox `ens1f0np0`：链路 UP、10 Gb/s、Full Duplex、autonegotiation on。独立 60 秒采样确认 RX CRC 与 symbol error 均增加 `8456`，即 `140.933333/s`。状态保持 P0：`NETWORK PHYSICAL FAULT PRESENT`。允许继续单机基础部署，但正式 NFS、第二节点、跨节点 NCCL 和高速共享存储均被阻止。未修改速率、FEC、autonegotiation、接口状态、固件或交换机。

## 已知 GPU 启动风险

- 当前旧 boot 日志包含四张 GPU 的 PCI DOE mailbox timeout
- 当前 `nouveau` 绑定阶段出现 AMD-Vi I/O page faults
- 新 boot 后必须检查 NVIDIA/NVML 一致性、4 张 H100、UUID、Xid、AER/PCIe、ECC、温度、功耗和拓扑
- MIG 仅查询能力；不启用、不创建实例
- NVIDIA 验收失败时停止 Docker、Slurm 及后续部署

## 回滚方式

以下仅为故障恢复说明，本阶段没有执行任何回滚命令：

1. SSH 无法恢复时，通过服务器本地或带外控制台进入 GRUB，选择 `Advanced options for Ubuntu` 下的 `7.0.0-14-generic`；不要操作 RAID、LVM 或文件系统格式。
2. 若 XFS 挂载因 fstab 参数失败，在恢复控制台用备份 `/srv/gpu-platform/platform/backups/fstab-20260804-050357` 恢复 `/etc/fstab`，保持 `root:root` 和 `0644`，然后再次运行 `findmnt --verify --verbose`。
3. 若 NVIDIA 新 boot 无法通过验收，先保留现场日志；从控制台进入可启动内核后，按安装日志移除本次 R595 compute-only 包与 pin，重建所有 initramfs。不得使用 `.run` 安装器或混入其他驱动来源。
4. 原始安装、模拟和 initramfs 日志均位于 `/srv/gpu-platform/platform/reports/`，可用于精确核对包事务。

## 最终 Gate

- `sudo sshd -t`：PASSED
- `sudo findmnt --verify --verbose`：PASSED
- `systemctl is-enabled ssh`：enabled
- `systemctl is-active ssh`：active
- `ip -br addr`：`10.82.36.1/24` 存在于 `ens1f0np0`
- `ip route`：默认路由存在
- `sudo -n true`：PASSED
- reboot：`NOT EXECUTED`

结论：已满足进入第一次重启审批 Gate 的前置条件；NVIDIA、project quota 均必须在重启后才能验收。
