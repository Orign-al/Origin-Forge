# H100 Phase P0-B 审批 Gate 状态

## 当前检查点

| 项目 | 值 |
|---|---|
| Run ID | `20260805-032025` |
| 开始时间 | `2026-08-05T03:18:46+08:00` |
| 起始 Git HEAD | `fc1e2cc17061cd0cd112fe14dd3521ea036e0cd3` |
| 主机/Kernel | `sagsh100server` / `7.0.0-28-generic` |
| 管理/默认路由/Mellanox 接口 | 均为 `ens1f0np0` |
| Mellanox PCI | `0000:61:00.0` |
| 第二 Mellanox 端口 | `ens1f1np1`，carrier 0、未连接 |
| 安全标记 | `MANAGEMENT DEPENDS ON FAULTY LINK` |

Slurm 保持 DRAIN、队列为空；四卡 MIG Disabled；只有 origin-al/codexops 两个交互用户；UFW inactive。

## 物理整改 Gate

整改前正式采样为 6 个快照、5 个区间：`03:23:40` 至 `03:28:41`，301 秒。RX 27,739 bytes、TX 4,869 bytes；CRC +530、symbol +530，平均各 1.760797/s；link-down、discard、corrected bits、module-unplug 增量为 0。`rx_uncorrected_phy` 和标准 sysfs `carrier_changes` 未暴露，记为 NA。

`03:43:59` collector 临时快照的 CRC 为 7,315,060，较正式窗口末值 4,504,136 又增加 2,810,924；约 918 秒平均 3,062.008715/s。期间只有 DNS/TLS 轻量测试和 3.3 MB Ubuntu 官方 shellcheck 下载，物理 P0 仍非常活跃。

现场尚未清洁/重插或更换任何光纤、DAC/AOC、模块、交换机端口、服务器端口或 NIC。没有 post-change 数据。管理员/现场人员必须先从 A–H 中明确一项并记录时间、人员、旧/新部件和交换机端口；管理链路切换必须具备独立恢复通道。

`PHYSICAL REMEDIATION WAITING FOR FIELD ACTION`

## Docker Hub 与 Resolver Gate

当前 DNS：`114.114.114.114`、`223.5.5.5`。10 轮当前解析均 NOERROR、无超时：

- Registry A 示例 `66.220.148.145`，后续 TCP 测试又解析到 `31.13.80.169`；
- Auth A 示例 `103.42.176.244`，后续 TCP 测试又解析到 `128.242.250.157`；
- CloudFront 正确得到 `d3ipk8a4rcghwr.cloudfront.net` 和 AWS CloudFront 地址；
- docker.io/hub.docker.com 也出现可疑或变化地址。

当前路径结果：

- Registry：TCP timeout；TLS 30 秒 timeout；无 HTTP；
- Auth：TCP timeout；TLS 31 秒 timeout；无 HTTP；
- CloudFront：TCP 成功；独立 TLS 1.3 成功，证书 `*.cloudfront.docker.com`，Amazon CA，verify OK；一次 curl GET 被 reset；
- NGC/GHCR/Quay：均返回预期 401，TLS 正常。

Resolver B 尚未提供，因此不能按本阶段标准最终确认 DNS 污染，也不能执行批准 IP 的 `curl --resolve`。没有选择 DNS、HTTPS proxy 或内部 Registry 方案；没有更改 DNS、代理、Docker daemon、MTU、IPv6 或 `/etc/hosts`，没有执行 Docker pull。

## Mellanox collector Gate

已从 Ubuntu 26.04 `resolute/universe` 官方仓库安装唯一新包 `shellcheck 0.11.0-2`；APT 未重启任何服务。候选 collector 已通过：

- `bash -n`；
- shellcheck；
- 5/5 指标结构检查；
- `promtool check metrics`；
- Node Exporter textfile flag 支持检查；
- `systemd-analyze verify`；
- 普通用户只读 ethtool 测试，确认不需要 CAP_NET_ADMIN。

生产目标全部不存在，Node Exporter 未修改/重启，生产 `/metrics` 尚无 `h100_mellanox_*`。必须等待原文批准：`允许部署 Mellanox Prometheus collector`。

## PCI DOE 与延后项

四张 H100 仍为 Gen5 x16，DCGM r1 四卡 Pass，无 Xid、实际 AER error 或掉卡证据，保持 `PCI DOE TIMEOUT — P1 OBSERVATION`。wait-online 按要求延后到两个 P0 完成后，未修改 netplan/networkd。

## 继续所需输入

1. 现场本轮动作 A–H 及完整记录；
2. 公司批准的 Resolver B IP/主机、允许协议（UDP/TCP 53 或 DoT）和所有者；
3. Resolver 对比后，从 A 干净 DNS、B HTTPS proxy、C 内部 Registry 中只选择一个；
4. 如批准生产 collector，明确回复：`允许部署 Mellanox Prometheus collector`。

```text
NETWORK PHYSICAL P0 OPEN
DOCKER HUB CONNECTIVITY P0 OPEN
MELLANOX MONITORING NOT DEPLOYED
SYSTEMD WAIT ONLINE P2 OPEN
PCI DOE STATUS:
P1 OBSERVATION
SLURM NODE REMAINS DRAINED
MIG NOT MODIFIED
FIREWALL NOT MODIFIED
OTHER USERS NOT CREATED
```

当前不满足 `READY FOR CONTROLLED USER ONBOARDING`。
